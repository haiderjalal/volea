# Supabase project hosting the `volea` schema.
# The schema must be listed under Dashboard -> Project Settings -> API -> Exposed schemas.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Absolute origin, used for auth redirects, canonical URLs and Open Graph.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
